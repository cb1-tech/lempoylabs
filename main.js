const config=window.LEMPOY_CONFIG||{};const learn=document.querySelector("[data-itala-link]");if(learn&&config.italaUrl)learn.href=config.italaUrl;const connect=document.querySelector("[data-connect]");const hello=document.querySelector("[data-say-hello]");const methods=[];if(config.email)methods.push(`<a href="mailto:${config.email}">${config.email}</a>`);if(config.instagramUrl)methods.push(`<a href="${config.instagramUrl}">Instagram</a>`);if(methods.length&&connect&&hello){connect.hidden=false;connect.insertAdjacentHTML("beforeend",methods.join(""));hello.hidden=false;hello.href=config.email?`mailto:${config.email}`:config.instagramUrl}document.querySelectorAll(".mobile-menu a").forEach(link=>link.addEventListener("click",()=>link.closest("details")?.removeAttribute("open")));
